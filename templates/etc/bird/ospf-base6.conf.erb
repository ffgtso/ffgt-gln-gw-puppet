# this file is generated by puppet

filter ex_ospf_<%= @name.gsub('-', '_')_ds {
    if (source = RTS_BGP) then reject;
    if (source = RTS_OSPF || source = RTS_STATIC || source = RTS_DEVICE) then accept;
    reject;
}

filter im_ospf_<%= @name.gsub('-', '_')_ds {
    #if is_default() then accept;
    if (net.len = 128) then accept;
    if (net.len >= 16 && net.len <= 64) then accept;
    reject;
}

filter ex_ospf_<%= @name.gsub('-', '_')_ds {
    if (source = RTS_BGP) then reject;
    if (is_default() && (source = RTS_BGP || source = RTS_STATIC || source = RTS_DEVICE)) then accept;
    if (source = RTS_OSPF || source = RTS_STATIC || source = RTS_DEVICE) then accept;
    reject;
}

filter im_ospf_<%= @name.gsub('-', '_')_ds {
    if is_default() then accept;
    if (net.len = 128) then accept;
    if (net.len >= 16 && net.len <= 64) then accept;
    reject;
}
