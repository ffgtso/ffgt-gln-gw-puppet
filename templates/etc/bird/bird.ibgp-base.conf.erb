# this is file is generated by puppet

# Do not accept anything smaller than /4.
function in_prefix_range_ibgp() {
  if net.len < 4 then return false;
  #if (net.len > 24 && net.len < 32) then return false;
  return true;
}
