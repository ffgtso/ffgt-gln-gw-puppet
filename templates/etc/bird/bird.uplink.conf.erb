protocol direct {
  interface "uplink-*";
  table mesh;
}

template bgp uplink {
  table mesh;
  local as <%= @icvpn_as %>;
  export where is_default();
  import where net ~ [ <%=@nat_prefix%>/<%=@nat_prefixlen%>+ ]";
  next hop self;
  multihop 64;
}
