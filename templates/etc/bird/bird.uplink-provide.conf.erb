table uplink;

protocol direct {
  table uplink;
  interface "uplink-*";
}

template bgp uplink {
  table uplink;
  local as <%= @uplink_as %>;
  import none;
  export where is_default();
  next hop self;
  multihop 64;
}
