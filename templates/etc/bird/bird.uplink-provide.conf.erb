table uplnk;

protocol direct {
  table uplnk;
  interface "uplink-*";
}

template bgp uplink {
  table uplnk;
  local as <%= @uplink_as %>;
  import none;
  export where is_default();
  next hop self;
  multihop 64;
}
